export default function Page() {
  return (
    <main style={{ padding: 24, fontFamily: "sans-serif" }}>
      <h1>BikeApp customer API</h1>
      <p>Backend is running. Register at POST /api/auth/register and log in at POST /api/auth/login.</p>
    </main>
  );
}
